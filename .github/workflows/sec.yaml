name: Sec

on:
  workflow_dispatch:
  
jobs:
  job1:
    env: 
      mykey: ${{ secrets.QA_KEY }}
    runs-on: ubuntu-latest
    steps:
      - name: Conditional Job 1
        run: cat $mykey > key.txt
      - name: writing
        run: cat key.txt
      
